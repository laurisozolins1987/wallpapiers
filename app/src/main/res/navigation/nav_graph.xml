<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.wallpapiers.ui.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/actionHomeFragmentToDetailFragment"
            app:destination="@id/detailFragment" />
        <action
            android:id="@+id/actionHomeFragmentToSearchFragment"
            app:destination="@id/searchFragment" />
        <action
            android:id="@+id/actionHomeFragmentToSettingsFragment"
            app:destination="@id/settingsFragment" />
    </fragment>

    <fragment
        android:id="@+id/detailFragment"
        android:name="com.example.wallpapiers.ui.DetailFragment"
        android:label="Detail"
        tools:layout="@layout/fragment_detail">
        <argument
            android:name="wallpaperId"
            app:argType="string" />
        <argument
            android:name="wallpaperPreviewUrl"
            app:argType="string" />
        <argument
            android:name="wallpaperOriginalUrl"
            app:argType="string" />
        <argument
            android:name="wallpaperPhotographer"
            app:argType="string" />
        <argument
            android:name="wallpaperTitle"
            app:argType="string" />
        <argument
            android:name="wallpaperSourceName"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.example.wallpapiers.ui.SearchFragment"
        android:label="Search"
        tools:layout="@layout/fragment_search">
        <argument
            android:name="initialQuery"
            app:argType="string"
            android:defaultValue="" />
        <argument
            android:name="initialTitle"
            app:argType="string"
            android:defaultValue="" />
        <argument
            android:name="openAsCategory"
            app:argType="boolean"
            android:defaultValue="false" />
        <action
            android:id="@+id/actionSearchFragmentToDetailFragment"
            app:destination="@id/detailFragment" />
    </fragment>

    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.example.wallpapiers.ui.SettingsFragment"
        android:label="Settings"
        tools:layout="@layout/fragment_settings" />

</navigation>
